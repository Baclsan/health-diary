export type HeadacheEntry = {
  id: string
  createdAt: string
  startedAt: string
  intensity: number
  durationMinutes: number
  location: string
  painType: string
  symptoms: string
  medication: string
  triggers: string
  comment: string
  worstHeadache: boolean
}
